{
  "devDependencies": {
    "@coral-xyz/anchor": "0.31.1",
    "@solana/spl-token": "^0.4.3",
    "@solana/web3.js": "^1.95.2",
    "@types/node": "^24.3.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "test": "ts-mocha -p tsconfig.json -t 1000000 programs/reward-pool/tests/**/*.ts",
    "predeploy:local": "node scripts/ensure-anchor-wallet.js",
    "deploy:local": "npm run predeploy:local && anchor deploy -p reward_pool",
    "build:local": "anchor build -p reward_pool"
  }
}
